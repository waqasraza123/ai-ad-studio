# Phase 24

Platform-specific render packs with per-network safe zones, caption layouts, and CTA timing presets.

## Acceptance criteria

- durable platform_render_packs table exists
- default render packs are seeded per owner
- project page shows active render pack summary for the current platform and aspect ratio
- render planning uses render-pack safe zones
- final composition uses render-pack caption layouts and CTA timing
- render metadata, usage events, and traces persist render pack information
