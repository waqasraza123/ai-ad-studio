# Phase 8

Real FFmpeg composition and first actual MP4 output.

## Acceptance criteria

- render_final_ad produces a real MP4 through FFmpeg
- export video asset is persisted with width, height, and duration metadata
- MP4 is uploaded to R2
- export detail page renders a playable video
- render mode is ffmpeg_static_composition
