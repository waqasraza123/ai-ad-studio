# Phase 3

Auth, schema, and repository foundation.

## Acceptance criteria

- Supabase auth integration points exist in the web app
- protected app shell checks authenticated user when auth is configured
- login route exists
- versioned schema and RLS live in repo SQL migrations
- typed repository foundations exist for projects
- repo stays green without pretending external services are already configured
