# Phase 13

Multi-format exports and platform presets.

## Acceptance criteria

- render_final_ad can produce multiple exports in one run
- supported export aspect ratios are 9:16, 1:1, and 16:9
- render flow supports platform presets that drive output formats and framing
- export records persist aspect_ratio and platform_preset
- project and export UI display preset and format metadata
